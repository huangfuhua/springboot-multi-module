#启动端口
server:
  port: 8989
  tomcat:
      uri-encoding: UTF-8
      max-threads: 1000
      min-spare-threads: 30

#数据库配置
spring:
  datasource:
  url: jdbc:mysql://localhost:3306/spring_multi_module?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
  username: root
  password: 131421
  driver-class-name: com.mysql.jdbc.Driver #驱动
  jpa:
    hibernate:
      ddl-auto: update #自动更新
    show-sql: true  #日志中显示sql语句
